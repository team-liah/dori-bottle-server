spring:
  datasource:
    url: jdbc:h2:mem:doribottle_test;DB_CLOSE_DELAY=-1;
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379
  cloud:
    aws:
      credentials:
        access-key: dummyKey
        secret-key: dummyKey

logging.level:
  org.hibernate.SQL: debug

app:
  auth:
    jwt:
      secret: dummyKeyC928D0F74BF2705749E0A843A4E716D0F147D6C6476534C3B1FBC4717C9B4539
      expiredMs: 3600000 # 1hour
      preAuthExpiredMs: 300000 # 5minute
    refreshToken:
      expiredMs: 1209600000 # 2weeks
  toast:
    sms:
      url: https://api-sms.cloud.toast.com/
      appKey: dummyKey
      secretKey: dummyKey
      sendNo: dummyNo
  aws:
    sqs:
      queueName: dummyQueue